#ifndef COMMANDS_HPP
#define COMMANDS_HPP

#include <vector>
#include "polygon.hpp"

namespace ibragimov
{
  double calculateArea(const std::vector< Polygon >&);
  // double findMax(const std::vector< Polygon >&);
  // double findMin(const std::vector< Polygon >&);
  // double count(const std::vector< Polygon >&);
}

#endif
